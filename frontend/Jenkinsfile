pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                script {
                    // Run a Docker container and execute a command inside it
                    docker.image('node:14').inside {
                        sh 'npm install'
                    }
                }
            }
        }
    }
}
