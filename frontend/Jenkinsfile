node {
    def dockerImage

    stage('Build Docker Image') {
        dockerImage = docker.build('my-docker-image', './path/to/Dockerfile')
    }

    stage('Publish Docker Image') {
        docker.withRegistry('https://docker.registry.url', 'docker-credentials-id') {
            dockerImage.push()
        }
    }
}
